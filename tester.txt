def valueChanged(tag, tagPath, previousValue, currentValue, initialChange, missedEvents):
	value = newValue.value
	
	mapping =  {3 : "hi"}
	
	if value in mapping:
	    string_value = mapping[value]
	    
	    try:
	        system.tag.writeBlocking(paths=["[default]tester"], values=[string_value])
	    
	    except Exception as e:
	        system.gui.messageBox("Error writing to tag: %s" % e)

Error writing tags Error writing to tag '[default]McConnellsburg/McConnellsburg/Frame Fab/Cell 2/State Output': Bad_NotFound

